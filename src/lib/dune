(library
  (name lib)
  (modules dict guess game utils solver wordle_functor config feedback)
  (modules_without_implementation config)
  (libraries core core_unix yojson)
  (preprocess (pps ppx_jane bisect_ppx))
  (flags (:standard -w -27-67))
)
